@src/schemas/comment.schema.ts @src/repository/comment.repository.ts @src/controller/comment.controller.ts @src/services/comment.service.ts @src/routes/comment.routes.ts see as in our project as you see we are convertion our code to bun js  using elysa , with postgress sql using drixzzel orma following repesotary  service pattren so write this comment code as  i provide you our lod code node js mongodb  code so convert it in that also we  have some diffent naming convention which is newcomment so now we are using in this  is comment in our project only use newcomment with the routes olny ass api call should be the same ok we are doing this beascuse our frontend is the same we dont want to change any thing from the frontend sid so use the eact varable for unique id use _id as in mongodb and al other proerty namr use the same as our frontend is the same so we dont wnat any check to brack as our old code is working perfectly ok so use same variable names ok for unique _id instead of using uuid use the bigint id with 5 digit random with auto incremental ok on every entery ok  so itell you aslo the primary key and the ownerid is uuid ok as that table aready createed with uuid ok old mongoodb and nodejs ==> // newComment.model.js - UPDATED SCHEMA // ============================================ import mongoose, { Schema } from "mongoose"; import mongooseAggregatePaginate from "mongoose-aggregate-paginate-v2"; import { getNextCommentSequence, CommentCounter } from "./commentCounter.model.js"; // Import the helper const newCommentSchema = new Schema( { inCommentId: { type: String, unique: true, // ✅ Ensure uniqueness index: true,  // ✅ Index for fast lookups }, // Optional text content content: { type: String, trim: true, default: null, }, // Auto-derived comment type commentType: { type: String, enum: [ "text", "audio", "video", "image", "sticker", "file", "text_audio", "text_video", "text_image", "text_sticker", "text_file", ], default: "text" }, // Media URLs (Cloudinary or similar) audioUrl: { type: String, default: null, }, videoUrl: { type: String, default: null, }, imageUrl: { type: String, default: null, }, stickerUrl: { type: String, default: null, }, fileUrl: { type: String, default: null, }, // Relations postId: { type: mongoose.Schema.Types.ObjectId, ref: "Post", required: true, index: true, }, // Pinned comment field pinned: { type: Boolean, default: false }, owner: { type: mongoose.Schema.Types.ObjectId, ref: "User", required: true, }, // Reply support parentComment: { type: mongoose.Schema.Types.ObjectId, ref: "NewComment", default: null, index: true, }, isReply: { type: Boolean, default: false, }, // Like / Dislike counters numberOfLikes: { type: Number, default: 0, min: 0, }, numberOfDislikes: { type: Number, default: 0, min: 0, }, // Track users who reacted likedBy: [ { type: mongoose.Schema.Types.ObjectId, ref: "User", }, ], dislikedBy: [ { type: mongoose.Schema.Types.ObjectId, ref: "User", }, ], }, { timestamps: true, } ); // Plugins newCommentSchema.plugin(mongooseAggregatePaginate); // Indexes newCommentSchema.index({ postId: 1, createdAt: -1 }); newCommentSchema.index({ owner: 1, createdAt: -1 }); newCommentSchema.index({ parentComment: 1, createdAt: -1 }); newCommentSchema.index({ likedBy: 1 }); newCommentSchema.index({ dislikedBy: 1 }); newCommentSchema.index({ postId: 1, isReply: 1, createdAt: -1 }); newCommentSchema.index({ postId: 1, parentComment: 1 }); newCommentSchema.index({ owner: 1 }); newCommentSchema.index({ contentId: 1, pinned: 1 }); // Index for pinned // ✅ PRE-SAVE HOOK: Auto-generate inCommentId + Auto-calculate commentType newCommentSchema.pre("save", async function (next) { try { // 1. Auto-generate inCommentId if new document if (this.isNew && !this.inCommentId) { const nextId = await getNextCommentSequence("commentId"); this.inCommentId = `${nextId}`; // Will be "26", "27", "28", etc. } // 2. Auto-calculate commentType if ( this.isNew || this.isModified("content") || this.isModified("audioUrl") || this.isModified("videoUrl") || this.isModified("imageUrl") || this.isModified("stickerUrl") || this.isModified("fileUrl") ) { const hasText = Boolean(this.content && this.content.trim()); const hasAudio = Boolean(this.audioUrl); const hasVideo = Boolean(this.videoUrl); const hasImage = Boolean(this.imageUrl); const hasSticker = Boolean(this.stickerUrl); const hasFile = Boolean(this.fileUrl); if (hasText && hasAudio) this.commentType = "text_audio"; else if (hasText && hasVideo) this.commentType = "text_video"; else if (hasText && hasImage) this.commentType = "text_image"; else if (hasText && hasFile) this.commentType = "text_file"; else if (hasText && hasSticker) this.commentType = "text_sticker"; else if (hasAudio) this.commentType = "audio"; else if (hasVideo) this.commentType = "video"; else if (hasImage) this.commentType = "image"; else if (hasFile) this.commentType = "file"; else if (hasSticker) this.commentType = "sticker"; else this.commentType = "text"; } next(); } catch (error) { next(error); } }); // Cascade delete replies when deleting a comment newCommentSchema.static("findByIdAndDelete", async function (id) { await this.deleteMany({ parentComment: id }); return this.findOneAndDelete({ _id: id }); }); // Toggle like newCommentSchema.methods.toggleLike = async function (userId) { const userIdStr = userId.toString(); const hasLiked = this.likedBy.some((id) => id.toString() === userIdStr); const hasDisliked = this.dislikedBy.some((id) => id.toString() === userIdStr); if (hasLiked) { this.likedBy = this.likedBy.filter((id) => id.toString() !== userIdStr); this.numberOfLikes = Math.max(0, this.numberOfLikes - 1); } else { this.likedBy.push(userId); this.numberOfLikes += 1; if (hasDisliked) { this.dislikedBy = this.dislikedBy.filter( (id) => id.toString() !== userIdStr ); this.numberOfDislikes = Math.max(0, this.numberOfDislikes - 1); } } return this.save(); }; // Toggle dislike newCommentSchema.methods.toggleDislike = async function (userId) { const userIdStr = userId.toString(); const hasLiked = this.likedBy.some((id) => id.toString() === userIdStr); const hasDisliked = this.dislikedBy.some((id) => id.toString() === userIdStr); if (hasDisliked) { this.dislikedBy = this.dislikedBy.filter( (id) => id.toString() !== userIdStr ); this.numberOfDislikes = Math.max(0, this.numberOfDislikes - 1); } else { this.dislikedBy.push(userId); this.numberOfDislikes += 1; if (hasLiked) { this.likedBy = this.likedBy.filter((id) => id.toString() !== userIdStr); this.numberOfLikes = Math.max(0, this.numberOfLikes - 1); } } return this.save(); }; export const NewComment = mongoose.model("NewComment", newCommentSchema); // ============================================ // INITIALIZATION SCRIPT - Run this ONCE to set counter to 25 // ============================================ // initCommentCounter.js or add to your server startup export async function initializeCommentCounter() { try { // Check if counter already exists const existingCounter = await CommentCounter.findById("commentId"); if (!existingCounter) { // Create counter starting at 25 (since you have 25 existing comments) await CommentCounter.create({ _id: "commentId", seq: 25 }); console.log("✅ Comment counter initialized at 25"); } else { console.log(`✅ Comment counter already exists at: ${existingCounter.seq}`); } } catch (error) { console.error("❌ Error initializing comment counter:", error); } } controller ==> import mongoose, { isValidObjectId } from "mongoose"; import { NewComment } from "../models/newcomment.model.js"; import { Post } from "../models/post.model.js"; import { ApiError } from "../utils/ApiErrors.js"; import { ApiResponse } from "../utils/ApiResponse.js"; import { asyncHandler } from "../utils/asyncHandler.js"; import { uploadResult } from "../utils/Claudnary.js"; const newAddComment = asyncHandler(async (req, res) => { const { postId } = req.params; const { content, pinned } = req.body; if (!isValidObjectId(postId)) { throw new ApiError(400, "Invalid post ID"); } // Detect inputs safely const hasText = typeof content === "string" && content.trim().length > 0; const hasAudio = Array.isArray(req.files?.audioComment); const hasVideo = Array.isArray(req.files?.videoComment); const hasSticker = Array.isArray(req.files?.sticker); const hasImage = Array.isArray(req.files?.imageComment); const hasFile = Array.isArray(req.files?.fileComment); // ✅ NEW const hasMedia = hasAudio || hasVideo || hasSticker || hasImage || hasFile; if (!hasText && !hasMedia) { throw new ApiError( 400, "Provide text or at least one media (audio/video/image/sticker/file)" ); } // Check post const postExists = await Post.exists({ _id: postId, isPublished: true }); if (!postExists) { throw new ApiError(404, "Post not found or not published"); } const shouldPin = pinned === true || pinned === "true"; // ✅ Check permission: Only post owner can pin comments if (shouldPin && post.owner.toString() !== userId.toString()) { throw new ApiError(403, "Only post owner can pin comments"); } // Upload media let audioUrl, videoUrl, stickerUrl, imageUrl, fileUrl; // ✅ NEW const uploadTasks = []; if (hasAudio) { uploadTasks.push( uploadResult(req.files.audioComment[0].path).then((r) => { if (!r?.url) throw new ApiError("Audio upload failed"); audioUrl = r.url; }) ); } if (hasVideo) { uploadTasks.push( uploadResult(req.files.videoComment[0].path, true).then((r) => { if (!r?.url) throw new ApiError("Video upload failed"); videoUrl = r.url; }) ); } if (hasSticker) { uploadTasks.push( uploadResult(req.files.sticker[0].path).then((r) => { if (!r?.url) throw new ApiError("Sticker upload failed"); stickerUrl = r.url; }) ); } if (hasImage) { uploadTasks.push( uploadResult(req.files.imageComment[0].path).then((r) => { if (!r?.url) throw new ApiError("Image upload failed"); imageUrl = r.url; }) ); } // ✅ NEW: File upload if (hasFile) { uploadTasks.push( uploadResult(req.files.fileComment[0].path).then((r) => { if (!r?.url) throw new ApiError("File upload failed"); fileUrl = r.url; }) ); } await Promise.all(uploadTasks); // ✅ If pinning is requested, unpin any existing pinned comment first if (shouldPin) { await NewComment.updateMany( { postId: new mongoose.Types.ObjectId(postId), pinned: true, isReply: false }, { $set: { pinned: false } } ); } // Create comment (NO commentType here - auto-calculated by pre-save hook) const comment = await NewComment.create({ content: hasText ? content : undefined, postId, owner: req.userVerfied._id, audioUrl, videoUrl, stickerUrl, imageUrl, fileUrl, // ✅ NEW pinned: shouldPin, // ✅ Set pinned status }); // Owner info (from auth) const owner = { _id: req.userVerfied._id, username: req.userVerfied.username, fullName: req.userVerfied.fullName, avatar: req.userVerfied.avatar, }; return res .status(200) .json( new ApiResponse( 201, { ...comment.toObject(), owner }, "Comment added to post successfully", shouldPin ? "Comment added and pinned successfully" : "Comment added to post successfully" ) ); }); // Update comment on a post const newUpdateComment = asyncHandler(async (req, res) => { const { commentId } = req.params; const { content } = req.body; // Validate input if (!content || !content.trim()) { throw new ApiError(400, "Comment content is required"); } if (!isValidObjectId(commentId)) { throw new ApiError(400, "Invalid comment ID"); } // Find comment and ensure it exists const comment = await NewComment.findOne({ _id: commentId, }); if (!comment) { throw new ApiError(404, "Comment not found"); } // Check if user is the owner of comment if (comment.owner.toString() !== req.userVerfied._id.toString()) { throw new ApiError(403, "You are not authorized to update this comment"); } // Prepare update const updatePayload = {}; if (content && content.trim()) { updatePayload.content = content; } // Optional media updates (replace if new files are provided) if (req.files?.audioComment && req.files.audioComment.length > 0) { const r = await uploadResult(req.files.audioComment[0].path); if (!r?.url) throw new ApiError(400, "Audio upload failed"); updatePayload.audioUrl = r.url; } if (req.files?.videoComment && req.files.videoComment.length > 0) { const r = await uploadResult(req.files.videoComment[0].path); if (!r?.url) throw new ApiError(400, "Video upload failed"); updatePayload.videoUrl = r.url; } if (req.files?.sticker && req.files.sticker.length > 0) { const r = await uploadResult(req.files.sticker[0].path); if (!r?.url) throw new ApiError(400, "Sticker upload failed"); updatePayload.stickerUrl = r.url; } if (Object.keys(updatePayload).length === 0) { throw new ApiError(400, "No updates provided"); } // Update comment const updatedComment = await NewComment.findByIdAndUpdate( commentId, updatePayload, { new: true } ).populate("owner", "username fullName avatar"); return res .status(200) .json( new ApiResponse(200, updatedComment, "Post comment updated successfully") ); }); // Delete comment from a post const newDeleteComment = asyncHandler(async (req, res) => { const { commentId } = req.params; const { postId } = req.body; if (!isValidObjectId(commentId) || !isValidObjectId(postId)) { throw new ApiError(400, "Invalid comment ID or post ID"); } // Find comment with specific query for post const comment = await NewComment.findOne({ _id: commentId, postId: postId, }); if (!comment) { throw new ApiError(404, "Comment not found"); } // Check if user is the owner of comment if (comment.owner.toString() !== req.userVerfied._id.toString()) { throw new ApiError(403, "You are not authorized to delete this comment"); } // Delete the comment and its replies using schema static await NewComment.findByIdAndDelete(commentId); return res .status(200) .json( new ApiResponse( 200, {}, "Post comment and its replies deleted successfully" ) ); }); // Get comments with ratings for a post const newGetCommentsWithRatings = asyncHandler(async (req, res) => { const { postId } = req.params; const { page = 1, limit = 10, sortBy = "createdAt", sortType = "desc", } = req.query; if (!isValidObjectId(postId)) { throw new ApiError(400, "Invalid post ID"); } // Check if post exists and is published const post = await Post.findOne({ _id: postId, isPublished: true }); if (!post) { throw new ApiError(404, "Post not found"); } const pageNumber = parseInt(page, 10); const limitNumber = parseInt(limit, 10); const skip = (pageNumber - 1) * limitNumber; // Aggregate pipeline to get comments and join with ratings const commentsWithRatings = await NewComment.aggregate([ { $match: { postId: new mongoose.Types.ObjectId(postId), isReply: false, // Only get top-level comments }, }, // Join with ratings collection { $lookup: { from: "ratings", let: { contentId: "$contentId", contentType: "$contentType", owner: "$owner", }, pipeline: [ { $match: { $expr: { $and: [ { $eq: ["$contentId", "$$contentId"] }, { $eq: ["$contentType", "$$contentType"] }, { $eq: ["$owner", "$$owner"] }, ], }, }, }, { $project: { rating: 1, _id: 0, }, }, ], as: "ratingInfo", }, }, // Join with users collection { $lookup: { from: "users", localField: "owner", foreignField: "_id", as: "owner", pipeline: [ { $project: { username: 1, fullName: 1, avatar: 1, }, }, ], }, }, // Format the output { $addFields: { owner: { $arrayElemAt: ["$owner", 0] }, rating: { $cond: [ { $gt: [{ $size: "$ratingInfo" }, 0] }, { $arrayElemAt: ["$ratingInfo.rating", 0] }, null, ], }, }, }, { $project: { ratingInfo: 0, // Remove the array }, }, // Sort, skip, limit { $sort: sortBy === "rating" && sortType ? { rating: sortType === "desc" ? -1 : 1 } : { [sortBy]: sortType === "desc" ? -1 : 1 }, }, { $skip: skip, }, { $limit: limitNumber, }, ]); // Get total count for pagination const totalComments = await NewComment.countDocuments({ postId: postId, isReply: false, }); // Calculate pagination info const totalPages = Math.ceil(totalComments / limitNumber); const hasNextPage = pageNumber < totalPages; const hasPrevPage = pageNumber > 1; return res.status(200).json( new ApiResponse( 200, { comments: commentsWithRatings, pagination: { page: pageNumber, limit: limitNumber, totalComments, totalPages, hasNextPage, hasPrevPage, }, }, "Post comments with ratings fetched successfully" ) ); }); const newAddReply = asyncHandler(async (req, res) => { const { commentId } = req.params; const { content } = req.body; if (!isValidObjectId(commentId)) { throw new ApiError(400, "Invalid comment ID"); } // Detect inputs safely const hasText = typeof content === "string" && content.trim().length > 0; const hasAudio = Array.isArray(req.files?.audioComment); const hasVideo = Array.isArray(req.files?.videoComment); const hasSticker = Array.isArray(req.files?.sticker); const hasImage = Array.isArray(req.files?.imageComment); // ✅ NEW const hasFile = Array.isArray(req.files?.fileComment); // ✅ NEW const hasMedia = hasAudio || hasVideo || hasSticker || hasImage || hasFile; if (!hasText && !hasMedia) { throw new ApiError( 400, "Provide text or at least one media (audio/video/image/sticker/file)" ); } // Find the parent comment and ensure it exists const parentComment = await NewComment.findOne({ _id: commentId, }); if (!parentComment) { throw new ApiError(404, "Parent comment not found"); } // Upload media let audioUrl, videoUrl, stickerUrl, imageUrl, fileUrl; // ✅ NEW const uploadTasks = []; if (hasAudio) { uploadTasks.push( uploadResult(req.files.audioComment[0].path).then((r) => { if (!r?.url) throw new ApiError("Audio upload failed"); audioUrl = r.url; }) ); } if (hasVideo) { uploadTasks.push( uploadResult(req.files.videoComment[0].path, true).then((r) => { if (!r?.url) throw new ApiError("Video upload failed"); videoUrl = r.url; }) ); } if (hasSticker) { uploadTasks.push( uploadResult(req.files.sticker[0].path).then((r) => { if (!r?.url) throw new ApiError("Sticker upload failed"); stickerUrl = r.url; }) ); } // ✅ NEW: Image upload if (hasImage) { uploadTasks.push( uploadResult(req.files.imageComment[0].path).then((r) => { if (!r?.url) throw new ApiError("Image upload failed"); imageUrl = r.url; }) ); } // ✅ NEW: File upload if (hasFile) { uploadTasks.push( uploadResult(req.files.fileComment[0].path).then((r) => { if (!r?.url) throw new ApiError("File upload failed"); fileUrl = r.url; }) ); } await Promise.all(uploadTasks); // Create the reply (NO commentType here - auto-calculated by pre-save hook) const reply = await NewComment.create({ content: hasText ? content : undefined, postId: parentComment.postId, owner: req.userVerfied._id, parentComment: commentId, isReply: true, audioUrl, videoUrl, stickerUrl, imageUrl, // ✅ NEW fileUrl, // ✅ NEW }); // Return reply with user info const populatedReply = await NewComment.findById(reply._id).populate( "owner", "username fullName avatar" ); return res .status(200) .json( new ApiResponse( 201, populatedReply, "Reply added to comment successfully" ) ); }); const newGetComments = asyncHandler(async (req, res) => { const { postId } = req.params; const { cursor = null, limit = 25, sortBy = "createdAt", sortType = "desc", includeReplies = "false", repliesLimit = 3, maxDepth = 3, includeGrandTotal = "false", getHasReply = "true", getRepliesCount = "false", userCommentId = null, pinnedComment = "true", // Boolean parameter to include pinned comment } = req.query; if (!isValidObjectId(postId)) { throw new ApiError(400, "Invalid post ID"); } console.log("Request Query Params:", req.query); const post = await Post.findOne({ _id: postId, isPublished: true }); if (!post) { throw new ApiError(404, "Post not found"); } const limitNumber = parseInt(limit, 10); const shouldIncludeReplies = includeReplies === "true"; const shouldIncludeGrandTotal = includeGrandTotal === "true"; const shouldGetHasReply = getHasReply === "true"; const shouldGetRepliesCount = getRepliesCount === "true"; const shouldGetPinnedComment = pinnedComment === "true"; const repliesLimitNumber = parseInt(repliesLimit, 10); const maxDepthNumber = parseInt(maxDepth, 10); const userId = req.userVerfied._id; const sortOptions = {}; sortOptions[sortBy || "createdAt"] = sortType === "desc" ? -1 : 1; // Build match condition for cursor-based pagination const matchCondition = { postId: new mongoose.Types.ObjectId(postId), isReply: false, }; // ✅ FIX: Exclude pinned comments from regular pagination // They will be fetched separately to avoid duplicates if (shouldGetPinnedComment) { matchCondition.pinned = { $ne: true }; } // If cursor exists, add cursor condition if (cursor) { const cursorComment = await NewComment.findOne({ inCommentId: cursor, postId: new mongoose.Types.ObjectId(postId), isReply: false, }).select("createdAt"); if (cursorComment) { // Add cursor condition based on sort direction if (sortType === "desc") { matchCondition.createdAt = { $lt: cursorComment.createdAt }; } else { matchCondition.createdAt = { $gt: cursorComment.createdAt }; } } } // Pipeline for top-level comments with cursor const topLevelPipeline = [ { $match: matchCondition, // ✅ Now excludes pinned comments }, { $lookup: { from: "users", localField: "owner", foreignField: "_id", as: "owner", pipeline: [ { $project: { username: 1, fullName: 1, avatar: 1, }, }, ], }, }, { $addFields: { owner: { $arrayElemAt: ["$owner", 0] }, userHasLiked: { $in: [new mongoose.Types.ObjectId(userId), "$likedBy"], }, userHasDisliked: { $in: [new mongoose.Types.ObjectId(userId), "$dislikedBy"], }, }, }, { $project: { likedBy: 0, dislikedBy: 0, }, }, { $sort: sortOptions, }, { $limit: limitNumber + 1, // Fetch one extra to check if there's a next page }, ]; const topLevelComments = await NewComment.aggregate(topLevelPipeline); // Check if there's a next page const hasNextPage = topLevelComments.length > limitNumber; // Remove the extra comment if it exists if (hasNextPage) { topLevelComments.pop(); } // Array to store priority comments in order const priorityComments = []; // ✅ 1. Handle pinned comment (highest priority) - always fetch separately if (shouldGetPinnedComment) { const pinnedCommentResult = await NewComment.aggregate([ { $match: { pinned: true, postId: new mongoose.Types.ObjectId(postId), isReply: false, }, }, { $limit: 1, // Stop after finding the first pinned comment }, { $lookup: { from: "users", localField: "owner", foreignField: "_id", as: "owner", pipeline: [ { $project: { username: 1, fullName: 1, avatar: 1, }, }, ], }, }, { $addFields: { owner: { $arrayElemAt: ["$owner", 0] }, userHasLiked: { $in: [new mongoose.Types.ObjectId(userId), "$likedBy"], }, userHasDisliked: { $in: [new mongoose.Types.ObjectId(userId), "$dislikedBy"], }, isPinned: true, // Add flag to identify pinned comment }, }, { $project: { likedBy: 0, dislikedBy: 0, }, }, ]); // If pinned comment exists, add it to priority comments if (pinnedCommentResult.length > 0) { priorityComments.push(pinnedCommentResult[0]); } } // 2. Handle user comment (second priority) - only if userCommentId is provided if (userCommentId && isValidObjectId(userCommentId)) { const targetUserId = new mongoose.Types.ObjectId(userCommentId); // Find if the user's comment exists in the current results const userCommentIndex = topLevelComments.findIndex( (comment) => comment.owner._id.toString() === targetUserId.toString() ); // If user's comment is found, remove it from array and add to priority if (userCommentIndex !== -1) { const userComment = topLevelComments.splice(userCommentIndex, 1)[0]; userComment.isUserComment = true; // Add flag to identify user's comment priorityComments.push(userComment); } // If user's comment is not in current page, fetch it separately else { const userCommentResult = await NewComment.aggregate([ { $match: { postId: new mongoose.Types.ObjectId(postId), isReply: false, owner: targetUserId, }, }, { $lookup: { from: "users", localField: "owner", foreignField: "_id", as: "owner", pipeline: [ { $project: { username: 1, fullName: 1, avatar: 1, }, }, ], }, }, { $addFields: { owner: { $arrayElemAt: ["$owner", 0] }, userHasLiked: { $in: [new mongoose.Types.ObjectId(userId), "$likedBy"], }, userHasDisliked: { $in: [new mongoose.Types.ObjectId(userId), "$dislikedBy"], }, isUserComment: true, // Add flag to identify user's comment }, }, { $project: { likedBy: 0, dislikedBy: 0, }, }, { $limit: 1, }, ]); // If user's comment exists, add it to priority comments if (userCommentResult.length > 0) { priorityComments.push(userCommentResult[0]); } } } // Combine priority comments with regular comments // Remove extra comments to maintain the limit const finalComments = [...priorityComments, ...topLevelComments].slice( 0, limitNumber ); // Get the next cursor (inCommentId of the last comment) const nextCursor = finalComments.length > 0 ? finalComments[finalComments.length - 1].inCommentId : null; // Fetch ALL replies in ONE query ONLY if includeReplies is true let commentsWithReplies = finalComments; // Fetch reply metadata (hasReply and/or repliesCount) if requested if ((shouldGetHasReply || shouldGetRepliesCount) && finalComments.length > 0) { const commentIds = finalComments.map((c) => c._id); const replyMetadata = await NewComment.aggregate([ { $match: { postId: new mongoose.Types.ObjectId(postId), isReply: true, parentComment: { $in: commentIds }, }, }, { $group: { _id: "$parentComment", replyCount: { $sum: 1 }, }, }, ]); // Create a map for quick lookup const replyMetadataMap = new Map( replyMetadata.map((item) => [item._id.toString(), item.replyCount]) ); // Attach metadata to comments commentsWithReplies = finalComments.map((comment) => { const commentId = comment._id.toString(); const count = replyMetadataMap.get(commentId) || 0; const metadata = {}; if (shouldGetHasReply) { metadata.hasReply = count > 0; } if (shouldGetRepliesCount) { metadata.repliesCount = count; } return { ...comment, ...metadata }; }); } if (shouldIncludeReplies && finalComments.length > 0) { const allReplies = await NewComment.aggregate([ { $match: { postId: new mongoose.Types.ObjectId(postId), isReply: true, }, }, { $lookup: { from: "users", localField: "owner", foreignField: "_id", as: "owner", pipeline: [ { $project: { username: 1, fullName: 1, avatar: 1, }, }, ], }, }, { $addFields: { owner: { $arrayElemAt: ["$owner", 0] }, userHasLiked: { $in: [new mongoose.Types.ObjectId(userId), "$likedBy"], }, userHasDisliked: { $in: [new mongoose.Types.ObjectId(userId), "$dislikedBy"], }, }, }, { $project: { likedBy: 0, dislikedBy: 0, }, }, { $sort: { createdAt: -1 }, }, ]); // Build tree structure in memory only when needed const buildCommentTree = (comments, replies, maxDepth) => { const replyMap = new Map(); replies.forEach((reply) => { const parentId = reply.parentComment.toString(); if (!replyMap.has(parentId)) { replyMap.set(parentId, []); } replyMap.get(parentId).push(reply); }); const attachReplies = (comment, depth = 0) => { if (depth >= maxDepth) { comment.replies = []; comment.replyCount = 0; return; } const commentId = comment._id.toString(); const directReplies = replyMap.get(commentId) || []; const limitedReplies = depth === 0 ? directReplies.slice(0, repliesLimitNumber) : directReplies; const countAllReplies = (replies) => { let count = replies.length; replies.forEach((reply) => { const nestedReplies = replyMap.get(reply._id.toString()) || []; count += countAllReplies(nestedReplies); }); return count; }; comment.replyCount = countAllReplies(directReplies); comment.replies = limitedReplies; limitedReplies.forEach((reply) => { attachReplies(reply, depth + 1); }); }; comments.forEach((comment) => { attachReplies(comment, 0); }); return comments; }; commentsWithReplies = buildCommentTree( commentsWithReplies, // Use the updated comments with metadata allReplies, maxDepthNumber ); } else { // Only add empty replies structure if replies were requested but none exist // If includeReplies is false, keep comments as is (with metadata if requested) if (shouldIncludeReplies) { commentsWithReplies = commentsWithReplies.map((c) => ({ ...c, replies: [], replyCount: 0, })); } } // Calculate total counts only when needed const countPromises = [ NewComment.countDocuments({ postId: postId, isReply: false, }), ]; if (shouldIncludeGrandTotal) { countPromises.push( NewComment.countDocuments({ postId: postId, }) ); } const counts = await Promise.all(countPromises); const totalComments = counts[0]; const grandTotal = shouldIncludeGrandTotal ? counts[1] : undefined; // Build cursor-based pagination response const paginationData = { limit: limitNumber, nextCursor: hasNextPage ? nextCursor : null, hasNextPage, totalComments, }; if (shouldIncludeGrandTotal) { paginationData.grandTotal = grandTotal; } return res.status(200).json( new ApiResponse( 200, { comments: commentsWithReplies, pagination: paginationData, }, "Post comments fetched successfully" ) ); }); //pinned comment const pinComment = asyncHandler(async (req, res) => { const { commentId } = req.params; // Comment ID to pin from URL params const { postId } = req.body; // Post ID from request body // Validate comment ID if (!isValidObjectId(commentId)) { throw new ApiError(400, "Invalid comment ID"); } // Validate post ID if (!isValidObjectId(postId)) { throw new ApiError(400, "Invalid post ID"); } // Find the comment to be pinned const comment = await NewComment.findOne({ _id: commentId, postId: new mongoose.Types.ObjectId(postId), isReply: false, // Only top-level comments can be pinned }); if (!comment) { throw new ApiError(404, "Comment not found or cannot be pinned (only top-level comments can be pinned)"); } // Check if user has permission to pin (e.g., post owner or admin) const post = await Post.findById(postId); if (!post) { throw new ApiError(404, "Post not found"); } const userId = req.userVerfied._id; if (post.owner.toString() !== userId.toString()) { throw new ApiError(403, "You don't have permission to pin comments on this post"); } // Check if comment is already pinned if (comment.pinned === true) { throw new ApiError(400, "Comment is already pinned"); } // Unpin any existing pinned comment for this post (only one can be pinned at a time) await NewComment.updateMany( { postId: new mongoose.Types.ObjectId(postId), pinned: true, isReply: false }, { $set: { pinned: false } } ); // Pin the selected comment by setting pinned = true comment.pinned = true; await comment.save(); return res.status(200).json( new ApiResponse(200, comment, "Comment pinned successfully") ); }); const unpinComment = asyncHandler(async (req, res) => { const { commentId } = req.params; const { postId } = req.body; // or req.query if (!isValidObjectId(commentId)) { throw new ApiError(400, "Invalid comment ID"); } if (!isValidObjectId(postId)) { throw new ApiError(400, "Invalid post ID"); } // Find the pinned comment const comment = await NewComment.findOne({ _id: commentId, postId: new mongoose.Types.ObjectId(postId), pinned: true, }); if (!comment) { throw new ApiError(404, "Pinned comment not found"); } // Check if user has permission const post = await Post.findById(postId); if (!post) { throw new ApiError(404, "Post not found"); } const userId = req.userVerfied._id; if (post.owner.toString() !== userId.toString()) { throw new ApiError(403, "You don't have permission to unpin comments on this post"); } // Unpin the comment comment.pinned = false; await comment.save(); return res.status(200).json( new ApiResponse(200, comment, "Comment unpinned successfully") ); }); const newGetReplies = asyncHandler(async (req, res) => { const { commentId } = req.params; const { cursor = null, limit = 10, sortBy = "createdAt", sortType = "desc", includeNestedReplies = "false", nestedLimit = 3, maxDepth = 2, getHasReply = "true", getRepliesCount = "false", getTotalCount = "false", userCommentId = null, pinnedReplyId = null, } = req.query; if (!isValidObjectId(commentId)) { throw new ApiError(400, "Invalid comment ID"); } // Check if parent comment exists const parentComment = await NewComment.findById(commentId); if (!parentComment) { throw new ApiError(404, "Parent comment not found"); } const limitNumber = parseInt(limit, 10); const shouldIncludeNestedReplies = includeNestedReplies === "true"; const shouldGetHasReply = getHasReply === "true"; const shouldGetRepliesCount = getRepliesCount === "true"; const shouldGetTotalCount = getTotalCount === "true"; const nestedLimitNumber = parseInt(nestedLimit, 10); const maxDepthNumber = parseInt(maxDepth, 10); const userId = req.userVerfied._id; // Prepare sort options const sortOptions = {}; if (sortBy) { sortOptions[sortBy] = sortType === "desc" ? -1 : 1; } else { sortOptions.createdAt = -1; } // Build match condition for cursor-based pagination const matchCondition = { parentComment: new mongoose.Types.ObjectId(commentId), isReply: true, }; // If cursor exists, add cursor condition if (cursor) { const cursorReply = await NewComment.findOne({ inCommentId: cursor, parentComment: new mongoose.Types.ObjectId(commentId), isReply: true, }).select("createdAt"); if (cursorReply) { if (sortType === "desc") { matchCondition.createdAt = { $lt: cursorReply.createdAt }; } else { matchCondition.createdAt = { $gt: cursorReply.createdAt }; } } } // Get replies with populated user info and like/dislike status const repliesAggregate = NewComment.aggregate([ { $match: matchCondition, }, { $lookup: { from: "users", localField: "owner", foreignField: "_id", as: "owner", pipeline: [ { $project: { username: 1, fullName: 1, avatar: 1, }, }, ], }, }, { $addFields: { owner: { $arrayElemAt: ["$owner", 0] }, userHasLiked: { $in: [new mongoose.Types.ObjectId(userId), "$likedBy"], }, userHasDisliked: { $in: [new mongoose.Types.ObjectId(userId), "$dislikedBy"], }, }, }, { $project: { likedBy: 0, dislikedBy: 0, }, }, { $sort: sortOptions, }, { $limit: limitNumber + 1, }, ]); const replies = await repliesAggregate.exec(); // Check if there's a next page const hasNextPage = replies.length > limitNumber; // Remove the extra reply if it exists if (hasNextPage) { replies.pop(); } // Array to store priority replies in order const priorityReplies = []; // 1. Handle pinned reply (highest priority) - only if pinnedReplyId is provided if (pinnedReplyId && isValidObjectId(pinnedReplyId)) { const targetReplyId = new mongoose.Types.ObjectId(pinnedReplyId); const pinnedReplyIndex = replies.findIndex( (reply) => reply._id.toString() === targetReplyId.toString() ); if (pinnedReplyIndex !== -1) { const pinnedReplyItem = replies.splice(pinnedReplyIndex, 1)[0]; pinnedReplyItem.isPinned = true; priorityReplies.push(pinnedReplyItem); } else { const pinnedReplyResult = await NewComment.aggregate([ { $match: { _id: targetReplyId, parentComment: new mongoose.Types.ObjectId(commentId), isReply: true, }, }, { $lookup: { from: "users", localField: "owner", foreignField: "_id", as: "owner", pipeline: [ { $project: { username: 1, fullName: 1, avatar: 1, }, }, ], }, }, { $addFields: { owner: { $arrayElemAt: ["$owner", 0] }, userHasLiked: { $in: [new mongoose.Types.ObjectId(userId), "$likedBy"], }, userHasDisliked: { $in: [new mongoose.Types.ObjectId(userId), "$dislikedBy"], }, isPinned: true, }, }, { $project: { likedBy: 0, dislikedBy: 0, }, }, { $limit: 1, }, ]); if (pinnedReplyResult.length > 0) { priorityReplies.push(pinnedReplyResult[0]); } } } // 2. Handle user reply (second priority) - only if userCommentId is provided if (userCommentId && isValidObjectId(userCommentId)) { const targetUserId = new mongoose.Types.ObjectId(userCommentId); const userReplyIndex = replies.findIndex( (reply) => reply.owner._id.toString() === targetUserId.toString() ); if (userReplyIndex !== -1) { const userReplyItem = replies.splice(userReplyIndex, 1)[0]; userReplyItem.isUserComment = true; priorityReplies.push(userReplyItem); } else { const userReplyResult = await NewComment.aggregate([ { $match: { parentComment: new mongoose.Types.ObjectId(commentId), isReply: true, owner: targetUserId, }, }, { $lookup: { from: "users", localField: "owner", foreignField: "_id", as: "owner", pipeline: [ { $project: { username: 1, fullName: 1, avatar: 1, }, }, ], }, }, { $addFields: { owner: { $arrayElemAt: ["$owner", 0] }, userHasLiked: { $in: [new mongoose.Types.ObjectId(userId), "$likedBy"], }, userHasDisliked: { $in: [new mongoose.Types.ObjectId(userId), "$dislikedBy"], }, isUserComment: true, }, }, { $project: { likedBy: 0, dislikedBy: 0, }, }, { $limit: 1, }, ]); if (userReplyResult.length > 0) { priorityReplies.push(userReplyResult[0]); } } } // Combine priority replies with regular replies const finalReplies = [...priorityReplies, ...replies].slice(0, limitNumber); // Get the next cursor (inCommentId of the last reply) const nextCursor = finalReplies.length > 0 ? finalReplies[finalReplies.length - 1].inCommentId : null; // Fetch reply metadata (hasReply and/or repliesCount) only if requested let repliesWithMetadata = finalReplies; if ((shouldGetHasReply || shouldGetRepliesCount) && finalReplies.length > 0) { const replyIds = finalReplies.map((r) => r._id); const replyMetadata = await NewComment.aggregate([ { $match: { isReply: true, parentComment: { $in: replyIds }, }, }, { $group: { _id: "$parentComment", replyCount: { $sum: 1 }, }, }, ]); const replyMetadataMap = new Map( replyMetadata.map((item) => [item._id.toString(), item.replyCount]) ); repliesWithMetadata = finalReplies.map((reply) => { const replyId = reply._id.toString(); const count = replyMetadataMap.get(replyId) || 0; const metadata = {}; if (shouldGetHasReply) { metadata.hasReply = count > 0; } if (shouldGetRepliesCount) { metadata.repliesCount = count; } return { ...reply, ...metadata }; }); } // Fetch nested replies only if includeNestedReplies is true let repliesWithNested = repliesWithMetadata; if (shouldIncludeNestedReplies && finalReplies.length > 0) { const allNestedReplies = await NewComment.aggregate([ { $match: { isReply: true, }, }, { $lookup: { from: "users", localField: "owner", foreignField: "_id", as: "owner", pipeline: [ { $project: { username: 1, fullName: 1, avatar: 1, }, }, ], }, }, { $addFields: { owner: { $arrayElemAt: ["$owner", 0] }, userHasLiked: { $in: [new mongoose.Types.ObjectId(userId), "$likedBy"], }, userHasDisliked: { $in: [new mongoose.Types.ObjectId(userId), "$dislikedBy"], }, }, }, { $project: { likedBy: 0, dislikedBy: 0, }, }, { $sort: { createdAt: -1 }, }, ]); const buildReplyTree = (replyList, allReplies, maxDepth) => { const replyMap = new Map(); allReplies.forEach((reply) => { const parentId = reply.parentComment.toString(); if (!replyMap.has(parentId)) { replyMap.set(parentId, []); } replyMap.get(parentId).push(reply); }); const attachNestedReplies = (reply, depth = 0) => { if (depth >= maxDepth) { reply.replies = []; reply.replyCount = 0; return; } const replyId = reply._id.toString(); const directReplies = replyMap.get(replyId) || []; const limitedReplies = depth === 0 ? directReplies.slice(0, nestedLimitNumber) : directReplies; const countAllReplies = (replyItems) => { let count = replyItems.length; replyItems.forEach((r) => { const nested = replyMap.get(r._id.toString()) || []; count += countAllReplies(nested); }); return count; }; reply.replyCount = countAllReplies(directReplies); reply.replies = limitedReplies; limitedReplies.forEach((r) => { attachNestedReplies(r, depth + 1); }); }; replyList.forEach((reply) => { attachNestedReplies(reply, 0); }); return replyList; }; repliesWithNested = buildReplyTree( repliesWithMetadata, allNestedReplies, maxDepthNumber ); } else { if (shouldIncludeNestedReplies) { repliesWithNested = repliesWithMetadata.map((r) => ({ ...r, replies: [], replyCount: 0, })); } } // Get total count only if requested let totalReplies; if (shouldGetTotalCount) { totalReplies = await NewComment.countDocuments({ parentComment: commentId, isReply: true, }); } // Build pagination response const paginationData = { limit: limitNumber, nextCursor: hasNextPage ? nextCursor : null, hasNextPage, }; if (shouldGetTotalCount) { paginationData.totalReplies = totalReplies; } return res.status(200).json( new ApiResponse( 200, { replies: repliesWithNested, pagination: paginationData, }, "Comment replies fetched successfully" ) ); }); const toggleCommentLike = asyncHandler(async (req, res) => { const { commentId } = req.params; if (!isValidObjectId(commentId)) { throw new ApiError(400, "Invalid comment ID"); } // Find the comment const comment = await NewComment.findById(commentId); if (!comment) { throw new ApiError(404, "Comment not found"); } // Toggle the like using the model method await comment.toggleLike(req.userVerfied._id); // Return updated counts and user's current state const userIdStr = req.userVerfied._id.toString(); const hasLiked = comment.likedBy.some((id) => id.toString() === userIdStr); const hasDisliked = comment.dislikedBy.some( (id) => id.toString() === userIdStr ); return res.status(200).json( new ApiResponse( 200, { commentId: comment._id, numberOfLikes: comment.numberOfLikes, numberOfDislikes: comment.numberOfDislikes, userHasLiked: hasLiked, userHasDisliked: hasDisliked, }, hasLiked ? "Comment liked successfully" : "Like removed successfully" ) ); }); // Toggle dislike on a comment const toggleCommentDislike = asyncHandler(async (req, res) => { const { commentId } = req.params; if (!isValidObjectId(commentId)) { throw new ApiError(400, "Invalid comment ID"); } // Find the comment const comment = await NewComment.findById(commentId); if (!comment) { throw new ApiError(404, "Comment not found"); } // Toggle the dislike using the model method await comment.toggleDislike(req.userVerfied._id); // Return updated counts and user's current state const userIdStr = req.userVerfied._id.toString(); const hasLiked = comment.likedBy.some((id) => id.toString() === userIdStr); const hasDisliked = comment.dislikedBy.some( (id) => id.toString() === userIdStr ); return res.status(200).json( new ApiResponse( 200, { commentId: comment._id, numberOfLikes: comment.numberOfLikes, numberOfDislikes: comment.numberOfDislikes, userHasLiked: hasLiked, userHasDisliked: hasDisliked, }, hasDisliked ? "Comment disliked successfully" : "Dislike removed successfully" ) ); }); // Get like/dislike status for current user on a comment const getCommentLikeStatus = asyncHandler(async (req, res) => { const { commentId } = req.params; if (!isValidObjectId(commentId)) { throw new ApiError(400, "Invalid comment ID"); } const comment = await NewComment.findById(commentId).select( "numberOfLikes numberOfDislikes likedBy dislikedBy" ); if (!comment) { throw new ApiError(404, "Comment not found"); } const userIdStr = req.userVerfied._id.toString(); const hasLiked = comment.likedBy.some((id) => id.toString() === userIdStr); const hasDisliked = comment.dislikedBy.some( (id) => id.toString() === userIdStr ); return res.status(200).json( new ApiResponse( 200, { commentId: comment._id, numberOfLikes: comment.numberOfLikes, numberOfDislikes: comment.numberOfDislikes, userHasLiked: hasLiked, userHasDisliked: hasDisliked, }, "Comment like status fetched successfully" ) ); }); // Single optimized comment search with intelligent ranking const searchComments = asyncHandler(async (req, res) => { const { postId } = req.params; const { searchTerm, cursor = null, limit = 25, includeReplies = "false", withUsername = "false", withContent = "false", caseSensitive = "false", minLikes = 0, commentType = null, // "text", "audio", "video", "sticker", "image", "file", etc. dateFrom = null, dateTo = null, sortBy = "relevance", // "relevance", "likes", "recent" } = req.query; // Validate inputs if (!isValidObjectId(postId)) { throw new ApiError(400, "Invalid post ID"); } if (!searchTerm || searchTerm.trim().length === 0) { throw new ApiError(400, "Search term is required"); } // Check if post exists const post = await Post.findOne({ _id: postId, isPublished: true }); if (!post) { throw new ApiError(404, "Post not found"); } const limitNumber = parseInt(limit, 10); const shouldIncludeReplies = includeReplies === "true"; const searchUsername = withUsername === "true"; const searchContent = withContent === "true"; const isCaseSensitive = caseSensitive === "true"; const minLikesNumber = parseInt(minLikes, 10) || 0; const userId = req.userVerfied._id; // Determine search scope based on parameters // Case 1: Both false (default) → Search in BOTH // Case 2: withContent=true only → Search ONLY in content // Case 3: withUsername=true only → Search ONLY in username // Case 4: Both true → Search in BOTH const shouldSearchContent = searchContent || (!searchUsername && !searchContent); const shouldSearchUsername = searchUsername || (!searchUsername && !searchContent); console.log("Search Config:", { searchTerm: searchTerm.trim(), withUsername, withContent, shouldSearchContent, shouldSearchUsername, }); // Prepare search regex - escape special regex characters const searchRegex = new RegExp( searchTerm.trim().replace(/[.*+?^${}()|[\]\\]/g, "\\$&"), isCaseSensitive ? "" : "i" ); // Build base match conditions const baseMatch = { postId: new mongoose.Types.ObjectId(postId), numberOfLikes: { $gte: minLikesNumber }, }; // Include/exclude replies if (!shouldIncludeReplies) { baseMatch.isReply = false; } // Filter by comment type if (commentType) { baseMatch.commentType = commentType; } // Date range filter if (dateFrom || dateTo) { baseMatch.createdAt = {}; if (dateFrom) { baseMatch.createdAt.$gte = new Date(dateFrom); } if (dateTo) { baseMatch.createdAt.$lte = new Date(dateTo); } } // Cursor handling for pagination let cursorCondition = {}; if (cursor) { const cursorComment = await NewComment.findOne({ inCommentId: cursor, }).select("_id createdAt numberOfLikes"); if (cursorComment) { if (sortBy === "recent") { cursorCondition = { createdAt: { $lt: cursorComment.createdAt } }; } else if (sortBy === "likes") { cursorCondition = { $or: [ { numberOfLikes: { $lt: cursorComment.numberOfLikes } }, { numberOfLikes: cursorComment.numberOfLikes, _id: { $gt: cursorComment._id }, }, ], }; } else { // For relevance sorting, use simple _id based cursor cursorCondition = { _id: { $gt: cursorComment._id } }; } } } // Aggregation pipeline for intelligent search const searchPipeline = [ { $match: { ...baseMatch, ...cursorCondition }, }, // Lookup user information { $lookup: { from: "users", localField: "owner", foreignField: "_id", as: "ownerInfo", pipeline: [ { $project: { username: 1, fullName: 1, avatar: 1, }, }, ], }, }, { $addFields: { owner: { $arrayElemAt: ["$ownerInfo", 0] }, }, }, // Calculate match scores and relevance { $addFields: { // Content match score contentMatchScore: { $cond: [ shouldSearchContent && { $regexMatch: { input: { $ifNull: ["$content", ""] }, regex: searchRegex, }, }, { $add: [ 100, // Base score for content match // Bonus for exact match { $cond: [ { $eq: [ { $toLower: { $trim: { input: { $ifNull: ["$content", ""] } } } }, { $toLower: searchTerm.trim() }, ], }, 200, // Large bonus for exact content match 0, ], }, // Bonus for match at start of content { $cond: [ { $regexMatch: { input: { $ifNull: ["$content", ""] }, regex: new RegExp(`^${searchTerm.trim().replace(/[.*+?^${}()|[\]\\]/g, "\\$&")}`, isCaseSensitive ? "" : "i"), }, }, 50, // Bonus for match at start 0, ], }, ], }, 0, ], }, // Username match score usernameMatchScore: { $cond: [ shouldSearchUsername && { $or: [ { $regexMatch: { input: { $ifNull: ["$owner.username", ""] }, regex: searchRegex, }, }, { $regexMatch: { input: { $ifNull: ["$owner.fullName", ""] }, regex: searchRegex, }, }, ], }, { $add: [ 75, // Base score for username match // Bonus for exact username match { $cond: [ { $or: [ { $eq: [ { $toLower: { $ifNull: ["$owner.username", ""] } }, { $toLower: searchTerm.trim() }, ], }, { $eq: [ { $toLower: { $ifNull: ["$owner.fullName", ""] } }, { $toLower: searchTerm.trim() }, ], }, ], }, 150, // Large bonus for exact username match 0, ], }, ], }, 0, ], }, // Combined match (both username and content match) // Only applicable when BOTH search options are enabled hasBothMatches: { $cond: [ // Only calculate if both search options are enabled shouldSearchContent && shouldSearchUsername, { $and: [ { $regexMatch: { input: { $ifNull: ["$content", ""] }, regex: searchRegex, }, }, { $or: [ { $regexMatch: { input: { $ifNull: ["$owner.username", ""] }, regex: searchRegex, }, }, { $regexMatch: { input: { $ifNull: ["$owner.fullName", ""] }, regex: searchRegex, }, }, ], }, ], }, false, // If not searching both, no "both match" bonus ], }, // User interaction status userHasLiked: { $in: [new mongoose.Types.ObjectId(userId), "$likedBy"], }, userHasDisliked: { $in: [new mongoose.Types.ObjectId(userId), "$dislikedBy"], }, }, }, // Calculate total relevance score { $addFields: { totalMatchScore: { $add: [ "$contentMatchScore", "$usernameMatchScore", // HUGE bonus if both username AND content match { $cond: [ "$hasBothMatches", 500, // Top priority for comments where both match 0, ], }, ], }, // Calculate engagement score (likes - dislikes) engagementScore: { $subtract: [ { $multiply: ["$numberOfLikes", 10] }, { $multiply: ["$numberOfDislikes", 5] }, ], }, }, }, // Calculate final ranking score based on sortBy { $addFields: { finalRankingScore: { $switch: { branches: [ { case: { $eq: [sortBy, "likes"] }, then: { $add: [ { $multiply: ["$numberOfLikes", 1000] }, "$totalMatchScore", ], }, }, { case: { $eq: [sortBy, "recent"] }, then: { $add: [ { $toLong: "$createdAt" }, "$totalMatchScore", ], }, }, ], default: { // relevance (default) $add: [ "$totalMatchScore", "$engagementScore", ], }, }, }, }, }, // Filter: Only include comments that actually match the search { $match: { $or: [ { contentMatchScore: { $gt: 0 } }, { usernameMatchScore: { $gt: 0 } }, ], }, }, // Sort by final ranking score { $sort: { finalRankingScore: -1, createdAt: -1, }, }, // Pagination { $limit: limitNumber + 1, // Fetch one extra to check if there's more }, // Clean up fields - remove internal calculation fields { $project: { ownerInfo: 0, likedBy: 0, dislikedBy: 0, contentMatchScore: 0, usernameMatchScore: 0, hasBothMatches: 0, totalMatchScore: 0, engagementScore: 0, finalRankingScore: 0, }, }, ]; const searchResults = await NewComment.aggregate(searchPipeline); // Check if there's a next page const hasNextPage = searchResults.length > limitNumber; // Remove the extra comment if it exists if (hasNextPage) { searchResults.pop(); } // Get the next cursor const nextCursor = searchResults.length > 0 ? searchResults[searchResults.length - 1].inCommentId : null; // Get total count of matching comments (for reference) const countPipeline = [ { $match: baseMatch, }, { $lookup: { from: "users", localField: "owner", foreignField: "_id", as: "ownerInfo", pipeline: [ { $project: { username: 1, fullName: 1, }, }, ], }, }, { $addFields: { owner: { $arrayElemAt: ["$ownerInfo", 0] }, }, }, { $match: { $or: [ ...(shouldSearchContent ? [{ content: searchRegex }] : []), ...(shouldSearchUsername ? [ { "owner.username": searchRegex }, { "owner.fullName": searchRegex }, ] : []), ], }, }, { $count: "total", }, ]; const countResult = await NewComment.aggregate(countPipeline); const totalMatches = countResult.length > 0 ? countResult[0].total : 0; console.log(`Search Results: Found ${searchResults.length} comments (Total: ${totalMatches})`); // Build response const paginationData = { limit: limitNumber, nextCursor: hasNextPage ? nextCursor : null, hasNextPage, totalMatches, searchTerm: searchTerm.trim(), }; return res.status(200).json( new ApiResponse( 200, { comments: searchResults, pagination: paginationData, searchConfig: { searchedIn: { content: shouldSearchContent, username: shouldSearchUsername, }, filters: { minLikes: minLikesNumber, commentType, dateFrom, dateTo, }, sortBy, }, }, `Found ${totalMatches} matching comment${totalMatches !== 1 ? "s" : ""}` ) ); }); export { searchComments, newGetComments, pinComment, unpinComment, newAddComment, newUpdateComment, newDeleteComment, newGetCommentsWithRatings, newAddReply, newGetReplies, toggleCommentLike, toggleCommentDislike, getCommentLikeStatus, }; routers==>import { Router } from "express"; import { newAddComment, newDeleteComment, newGetComments, newGetCommentsWithRatings, newUpdateComment, newAddReply, // New function newGetReplies, // New function pinComment, unpinComment, searchComments } from "../controllers/newcomment.controller.js"; import { toggleCommentLike, toggleCommentDislike, getCommentLikeStatus, } from "../controllers/newcomment.controller.js"; import verifyJWT from "../middlewares/auth.middleware.js"; import { upload } from "../middlewares/multer.middleware.js"; const router = Router(); router.use(verifyJWT); // Apply verifyJWT middleware to all routes in this file // Comment routes // router //   .route("/getallcomments/:postId") //   .get(newGetComments) router .route("/:postId") .get(newGetComments) .post( upload.fields([ { name: "audioComment", maxCount: 1 }, { name: "videoComment", maxCount: 1 }, { name: "sticker", maxCount: 1 }, { name: "fileComment", maxCount: 1 }, { name: "imageComment", maxCount: 1 }, // ✅ NEW ]), newAddComment ); // Route to pin a comment router.route("/comment/:commentId/pin").post(verifyJWT, pinComment); // Route to unpin a comment router.route("/comment/:commentId/unpin").post(verifyJWT, unpinComment); // Comment with ratings route router.route("/with-ratings/:postId").get(newGetCommentsWithRatings); // Comment operations routes router .route("/:commentId") .patch( upload.fields([ { name: "audioComment", maxCount: 1 }, { name: "videoComment", maxCount: 1 }, { name: "sticker", maxCount: 1 }, ]), newUpdateComment ) .delete(newDeleteComment); // Reply routes router.route("/:commentId/replies").get(newGetReplies); router.route("/:commentId/reply").post( upload.fields([ { name: "audioComment", maxCount: 1 }, { name: "videoComment", maxCount: 1 }, { name: "sticker", maxCount: 1 }, { name: "fileComment", maxCount: 1 }, { name: "imageComment", maxCount: 1 }, ]), newAddReply ); // LIKE/DISLIKE ROUTES // ============================================ // Toggle like on a comment router.route("/comment/:commentId/like").post(toggleCommentLike); // Toggle dislike on a comment router.route("/comment/:commentId/dislike").post(toggleCommentDislike); // Get like/dislike status for current user router.route("/comment/:commentId/like-status").get(getCommentLikeStatus); //search comment router.route("/search/:postId").get(searchComments); export default router; dont forget the reply asls do it with that also as i have not tell you so do this ok